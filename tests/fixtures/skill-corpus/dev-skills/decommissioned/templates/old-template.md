# Stale template

This template was orphaned when the parent skill was decommissioned —
the SKILL.md was removed but supplementary files were left behind.

Drill's `missing_file` check should flag the parent folder as missing
SKILL.md. Phase 1.1 will add an "orphan supplementary files" check
that recognizes this scenario explicitly.
